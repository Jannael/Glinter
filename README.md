# Glinter
Glinter is a git plugin to enable a wrapper for git add, when you use "git add" with no arguments, it will open a GUI to select the files to add.

it also has a feature to avoid uploading the .env file to commit.
